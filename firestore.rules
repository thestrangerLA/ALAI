rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Warning: This rule allows open access and should be secured before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
